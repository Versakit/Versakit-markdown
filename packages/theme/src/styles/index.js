import defaultThemeStyles from '../themes/defaultTheme.css'
import customThemeStyles from '../themes/customTheme.css'

const themes = {
  default: defaultThemeStyles,
  custom: customThemeStyles,
}

export default themes
